# Stock Advisor v3.0 测试计划

## 文档信息
- **项目名称**: Stock Advisor v3.0 (智能缓存 + AI问答)
- **测试专家**: Test Expert
- **创建日期**: 2026-02-09
- **版本**: v3.0
- **测试环境**:
  - 后端: https://stock-advisor-api-6vtb.onrender.com
  - 前端: https://stock-advisor.netlify.app
  - Supabase: 生产数据库

---

## 1. 测试策略概述

### 1.1 测试范围

本次测试覆盖 v3.0 的两大新功能：

**功能一：智能缓存机制**
- 分层缓存架构 (内存 + Supabase)
- 缓存命中/未命中逻辑
- 缓存过期策略 (30秒/4小时/交易日判断)
- 批量行情 API
- 缓存预热调度任务
- 前端缓存状态显示
- Render 休眠后恢复

**功能二：AI 股票问答**
- 预设问题模板 (5种)
- 自由提问
- Token 成本控制
- 频率限制 (每日10次)
- 相似问题缓存
- 问答历史查询
- GLM-4 降级处理
- 前端 StockChatPanel 组件

### 1.2 测试方法

| 测试类型 | 工具/方法 | 覆盖范围 |
|---------|----------|---------|
| 功能测试 | Python requests + 手动 | 全部新 API |
| 缓存验证 | 时间戳对比 | 缓存有效性逻辑 |
| 性能测试 | 响应时间监控 | 缓存命中 vs 未命中 |
| 边界测试 | 手动构造 | 过期边界、频率限制 |
| 集成测试 | E2E 脚本 | 前后端联调 |
| 调度任务测试 | cron 模拟 | 缓存预热 |

### 1.3 风险区域

| 风险区域 | 优先级 | 影响 | 缓解措施 |
|---------|-------|------|---------|
| 缓存数据不一致 | 高 | 用户看到过时数据 | 缓存时间戳验证、手动刷新 |
| Supabase 延迟过高 | 中 | 缓存优化失效 | 监控延迟、内存缓存兜底 |
| 问答频率控制失效 | 中 | Token 成本失控 | 全局 Token 上限 |
| 缓存预热任务失败 | 中 | 首次访问慢 | 任务失败告警 |
| 相似问题匹配误判 | 低 | 重复调用 GLM | 关键词匹配阈值调整 |
| Render 休眠后缓存丢失 | 高 | 内存缓存失效 | 持久化到 Supabase |

### 1.4 质量门控标准

**生产就绪标准**:
- [ ] P0 测试用例通过率 100%
- [ ] P1 测试用例通过率 ≥ 95%
- [ ] 缓存命中时响应 < 1 秒
- [ ] 缓存未命中时响应 < 15 秒 (含 AI 分析)
- [ ] 问答功能可用率 > 95%
- [ ] Token 监控准确率 100%
- [ ] 无数据一致性问题
- [ ] 无内存泄漏

---

## 2. 测试环境需求

### 2.1 后端测试环境
- **部署平台**: Render (Free Tier 512MB)
- **Python 版本**: 3.11+
- **新增依赖**: 无 (复用现有)
- **环境变量**:
  - `SUPABASE_URL` (已有)
  - `SUPABASE_KEY` (已有)
  - `GLM_API_KEY` (已有)

### 2.2 数据库测试环境
- **Supabase 新增表**:
  - `stock_analysis_cache` (完整分析缓存)
  - `stock_kline_cache` (K线数据缓存)
  - `stock_chat_history` (问答历史)

### 2.3 测试数据要求
- **真实股票代码**: 600519, 000001, 512930
- **市场时间**:
  - 交易时段: 9:30-11:30, 13:00-15:00
  - 收盘后: 15:00-次日9:30
  - 非交易日: 周末、节假日
- **问答测试数据**:
  - 预设问题模板 (5种)
  - 长问题 (200字边界)
  - 相似问题对

---

## 3. 测试套件分类

### 3.1 冒烟测试套件 (Smoke Test Suite)
**目的**: 验证缓存和问答基本可用性
**执行时机**: 每次部署后
**预期时间**: 10 分钟

| 测试用例 | 优先级 | 预期结果 |
|---------|-------|---------|
| TC-V3-SMOKE-001 | P0 | 缓存表创建成功 |
| TC-V3-SMOKE-002 | P0 | 首次查询写入缓存 |
| TC-V3-SMOKE-003 | P0 | 缓存命中响应 < 1s |
| TC-V3-SMOKE-004 | P0 | 批量行情 API 成功 |
| TC-V3-SMOKE-005 | P0 | AI 问答接口可用 |

### 3.2 完整回归测试套件 (Full Regression Suite)
**目的**: 全面验证缓存和问答功能
**执行时机**: 版本发布前
**预期时间**: 90 分钟

包含以下子套件:
- 缓存功能测试 (25 用例)
- 问答功能测试 (20 用例)
- 性能测试 (10 用例)
- 边界测试 (15 用例)
- 调度任务测试 (5 用例)

### 3.3 端到端场景测试套件 (E2E Scenario Suite)
**目的**: 验证完整用户旅程
**执行时机**: 发布前最终验证

| 场景 | 用户目标 | 关键步骤 |
|-----|---------|---------|
| E2E-V3-001 | 用户体验缓存加速 | 首次访问(慢) → 刷新页面(快) |
| E2E-V3-002 | 用户使用 AI 问答 | 详情页 → 点击预设问题 → 获得回答 |
| E2E-V3-003 | 用户达到问答限额 | 连续提问10次 → 看到额度提示 |

---

## 4. 详细测试用例

## 测试套件 A: 智能缓存机制测试

---

### TC-CACHE-001: 缓存表结构验证

**优先级**: P0 - Critical
**类别**: 缓存/数据库
**功能**: 验证新增表结构正确

#### 前置条件
- Supabase 数据库可访问
- 已执行建表 SQL

#### 测试步骤
1. 连接到 Supabase
2. 查询 `stock_analysis_cache` 表结构
3. 查询 `stock_kline_cache` 表结构
4. 查询 `stock_chat_history` 表结构
5. 验证字段类型和索引

#### 预期结果
- `stock_analysis_cache` 包含字段: `code`, `name`, `price`, `indicators`, `ai_summary`, `full_analysis_updated_at` 等
- `stock_kline_cache` 包含字段: `code`, `trade_date`, `open_price`, `close_price` 等
- `stock_chat_history` 包含字段: `id`, `code`, `question`, `answer`, `tokens_used` 等
- 所有索引创建成功

#### 潜在失败点
- SQL 语法错误
- 字段类型不匹配
- 索引创建失败

---

### TC-CACHE-002: 首次查询写入缓存

**优先级**: P0 - Critical
**类别**: 缓存/写入
**功能**: 验证首次查询自动写入缓存

#### 前置条件
- 后端服务运行
- 数据库中 `stock_analysis_cache` 无目标股票记录

#### 测试步骤
1. 清空 `stock_analysis_cache` 表中 600519 的记录
2. 调用 `GET /api/v1/stock/600519`
3. 等待响应返回
4. 查询数据库 `stock_analysis_cache` 表
5. 验证记录已写入

#### 预期结果
```sql
SELECT * FROM stock_analysis_cache WHERE code = '600519';
```
- 返回 1 条记录
- `full_analysis_updated_at` 是最近时间
- `price`, `indicators`, `ai_summary` 等字段非空
- `quote_updated_at` 与 `full_analysis_updated_at` 接近

#### 边界情况
- 数据库写入失败时，API 仍返回数据
- 部分字段缺失时，写入 null

---

### TC-CACHE-003: 缓存命中逻辑验证

**优先级**: P0 - Critical
**类别**: 缓存/读取
**功能**: 验证缓存命中时不重复获取数据

#### 前置条件
- 数据库中已有 600519 的缓存记录 (4小时内)

#### 测试步骤
1. 第一次调用 `GET /api/v1/stock/600519?refresh=false`
2. 记录响应时间和 `cache_info` 字段
3. 等待 5 秒
4. 第二次调用 `GET /api/v1/stock/600519?refresh=false`
5. 对比两次响应的 `full_analysis_updated_at`

#### 预期结果
- 响应中 `cache_info.cached = true`
- 响应时间 < 1 秒
- 两次响应的 `full_analysis_updated_at` 相同 (未重新分析)
- 两次响应的 `price` 可能不同 (实时价格更新)

#### 数据验证
```json
{
  "cache_info": {
    "cached": true,
    "analysis_age_minutes": 15,
    "quote_age_seconds": 5,
    "next_refresh_hint": "数据缓存中,点击刷新获取最新分析"
  }
}
```

---

### TC-CACHE-004: 缓存未命中逻辑验证

**优先级**: P0 - Critical
**类别**: 缓存/未命中
**功能**: 验证缓存不存在或过期时重新获取

#### 测试步骤
1. 清空 `stock_analysis_cache` 表中 600519 的记录
2. 调用 `GET /api/v1/stock/600519?refresh=false`
3. 验证响应时间和 `cache_info`

#### 预期结果
- 响应时间 15-30 秒 (含完整分析)
- `cache_info.cached = false`
- 数据库中写入新记录

---

### TC-CACHE-005: 手动刷新功能

**优先级**: P0 - Critical
**类别**: 缓存/刷新
**功能**: 验证 `refresh=true` 强制重新分析

#### 前置条件
- 数据库中已有缓存记录

#### 测试步骤
1. 调用 `GET /api/v1/stock/600519?refresh=false`
2. 记录 `full_analysis_updated_at` 为 T1
3. 立即调用 `GET /api/v1/stock/600519?refresh=true`
4. 记录 `full_analysis_updated_at` 为 T2
5. 验证 T2 > T1

#### 预期结果
- `refresh=true` 时响应时间 15-30 秒
- `full_analysis_updated_at` 更新为最新
- 数据库中记录被更新

---

### TC-CACHE-006: 实时价格缓存 (30秒)

**优先级**: P0 - Critical
**类别**: 缓存/实时价格
**功能**: 验证实时价格内存缓存 30 秒

#### 测试步骤
1. 调用 `GET /api/v1/stock/600519` 获取价格
2. 立即再次调用，记录响应时间
3. 等待 35 秒
4. 第三次调用，记录响应时间

#### 预期结果
- 第二次调用响应时间 < 0.5 秒 (内存缓存命中)
- 第三次调用响应时间 1-2 秒 (需重新获取实时价格)
- `quote_updated_at` 在第三次调用后更新

---

### TC-CACHE-007: 批量行情 API 测试

**优先级**: P0 - Critical
**类别**: 缓存/批量 API
**功能**: 验证批量获取多只股票实时价格

#### 测试步骤
1. 调用 `GET /api/v1/quotes/batch?codes=600519,000001,512930`
2. 验证响应格式
3. 验证价格数据完整性

#### 预期结果
```json
{
  "codes": ["600519", "000001", "512930"],
  "count": 3,
  "quotes": {
    "600519": {
      "code": "600519",
      "name": "贵州茅台",
      "price": 1515.01,
      "change": 8.14,
      "volume": 25000,
      "updated_at": "2026-02-09T14:30:00Z"
    },
    "000001": { ... },
    "512930": { ... }
  },
  "elapsed": 1.5
}
```

- 响应时间 < 3 秒 (单次 HTTP 请求)
- 所有股票价格都返回
- 对比单独查询，性能提升明显

---

### TC-CACHE-008: 批量行情替代串行查询

**优先级**: P1 - High
**类别**: 缓存/性能优化
**功能**: 验证推荐列表使用批量 API

#### 测试步骤
1. 调用 `GET /api/v1/recommendations`
2. 验证返回 10 只股票的价格都是最新的
3. 记录响应时间

#### 预期结果
- 响应时间 < 3 秒 (原来 11-21 秒)
- 所有推荐股票包含实时价格
- 后端日志显示调用批量 API

---

### TC-CACHE-009: 缓存过期策略 - 4小时过期

**优先级**: P0 - Critical
**类别**: 缓存/过期策略
**功能**: 验证分析结果 4 小时后过期

#### 测试步骤
1. 在数据库中手动插入一条记录，`full_analysis_updated_at = NOW() - INTERVAL '5 hours'`
2. 调用 `GET /api/v1/stock/600519?refresh=false`
3. 验证是否重新分析

#### 预期结果
- 缓存被判定为过期
- 触发重新分析
- `full_analysis_updated_at` 更新为最新

#### 测试变体
| 缓存年龄 | 预期行为 |
|---------|---------|
| 1 小时 | 命中缓存 |
| 3.5 小时 | 命中缓存 |
| 4.5 小时 | 过期，重新分析 |
| 24 小时 | 过期，重新分析 |

---

### TC-CACHE-010: 缓存过期策略 - 实时价格 30 秒

**优先级**: P0 - Critical
**类别**: 缓存/过期策略
**功能**: 验证实时价格 30 秒过期

#### 测试步骤
1. 第一次调用获取价格，记录时间 T1
2. T1 + 25 秒调用，验证缓存命中
3. T1 + 35 秒调用，验证缓存过期

#### 预期结果
- 25 秒时缓存命中，响应 < 0.5 秒
- 35 秒时缓存过期，重新获取价格，响应 1-2 秒

---

### TC-CACHE-011: 交易日 vs 非交易日缓存策略

**优先级**: P1 - High
**类别**: 缓存/时段判断
**功能**: 验证非交易时段价格不过期

#### 测试场景

**场景1: 盘中 (交易时段)**
- 时间: 周一 10:00
- 缓存年龄: 40 秒
- 预期: 过期，重新获取

**场景2: 收盘后**
- 时间: 周一 16:00
- 缓存年龄: 2 小时
- 预期: 不过期，收盘价不变

**场景3: 周末**
- 时间: 周六 10:00
- 缓存年龄: 24 小时
- 预期: 不过期，返回周五收盘价

#### 测试步骤
1. 手动调整系统时间或在对应时间执行
2. 调用 API 验证缓存逻辑
3. 验证 `quote_updated_at` 是否更新

---

### TC-CACHE-012: 缓存预热调度任务

**优先级**: P1 - High
**类别**: 缓存/调度任务
**功能**: 验证 17:10 缓存预热任务

#### 前置条件
- 17:00 推荐生成任务已完成
- 自选股列表有数据

#### 测试步骤
1. 手动触发 `cache_warm_job` (或等待 17:10)
2. 监控任务执行日志
3. 验证数据库中缓存记录更新

#### 预期结果
- 任务扫描推荐股 + 自选股
- 逐个生成完整分析并写入 `stock_analysis_cache`
- 批量更新实时价格
- 执行时间 < 5 分钟 (假设 20 只股票)
- 任务失败时有告警日志

---

### TC-CACHE-013: 缓存预热覆盖范围

**优先级**: P1 - High
**类别**: 缓存/预热
**功能**: 验证预热覆盖推荐股 + 自选股

#### 测试步骤
1. 确保数据库中有 10 只推荐股和 5 只自选股
2. 执行缓存预热任务
3. 查询 `stock_analysis_cache` 表

#### 预期结果
```sql
SELECT COUNT(*) FROM stock_analysis_cache WHERE full_analysis_updated_at > NOW() - INTERVAL '10 minutes';
```
- 返回至少 15 条记录
- 所有推荐股和自选股都被预热

---

### TC-CACHE-014: K 线数据缓存

**优先级**: P1 - High
**类别**: 缓存/K 线
**功能**: 验证历史 K 线缓存到数据库

#### 测试步骤
1. 清空 `stock_kline_cache` 表中 600519 的记录
2. 调用 `GET /api/v1/stock/600519/kline?days=60`
3. 验证数据库中写入 60 条记录
4. 再次调用 API，验证从缓存读取

#### 预期结果
- 首次调用写入 60 条 K 线记录到 `stock_kline_cache`
- 第二次调用直接从缓存读取，响应时间 < 1 秒
- K 线数据不变

---

### TC-CACHE-015: 缓存一致性验证

**优先级**: P0 - Critical
**类别**: 缓存/一致性
**功能**: 验证手动刷新后缓存同步更新

#### 测试步骤
1. 调用 `GET /api/v1/stock/600519?refresh=false` (缓存命中)
2. 记录 `ai_summary` 字段内容为 V1
3. 调用 `GET /api/v1/stock/600519?refresh=true` (强制刷新)
4. 记录 `ai_summary` 字段内容为 V2
5. 再次调用 `refresh=false`，验证返回 V2

#### 预期结果
- 刷新后缓存立即更新
- 后续查询返回最新缓存
- 无脏数据

---

### TC-CACHE-016: Render 休眠后缓存恢复

**优先级**: P0 - Critical
**类别**: 缓存/休眠恢复
**功能**: 验证后端休眠后内存缓存丢失但 Supabase 缓存可用

#### 测试步骤
1. 等待 Render 服务休眠 (15 分钟无请求)
2. 调用 `GET /api/v1/stock/600519?refresh=false`
3. 验证缓存从 Supabase 恢复

#### 预期结果
- 冷启动耗时 < 90 秒
- 缓存从 Supabase 读取成功
- `cache_info.cached = true`
- 响应时间 < 5 秒 (含冷启动)

---

### TC-CACHE-017: 前端缓存提示 UI

**优先级**: P1 - High
**类别**: 缓存/前端显示
**功能**: 验证前端显示缓存状态

#### 测试步骤
1. 访问 `/stock/600519` 页面
2. 检查页面上的缓存提示

#### 预期元素
- 顶部 badge: "缓存数据 | 45分钟前更新" (灰色) 或 "实时分析 | 刚刚更新" (绿色)
- 右上角 [刷新分析] 按钮
- 点击后变为 [分析中...] + 进度条
- 刷新完成后 badge 更新为 "刚刚更新"

---

### TC-CACHE-018: 自选股列表快速加载

**优先级**: P1 - High
**类别**: 缓存/列表优化
**功能**: 验证自选股列表使用缓存 + 批量价格

#### 测试步骤
1. 添加 5 只股票到自选股
2. 访问自选股列表页
3. 记录加载时间

#### 预期结果
- 加载时间 < 3 秒 (原来 75-150 秒)
- 显示所有自选股的实时价格
- 显示缓存的分析数据
- 每只股票卡片显示 "分析更新于 X 分钟前"

---

### TC-CACHE-019: 缓存写入失败降级

**优先级**: P1 - High
**类别**: 缓存/容错
**功能**: 验证缓存写入失败时 API 仍正常返回

#### 测试步骤
1. 模拟 Supabase 写入失败 (断网或权限错误)
2. 调用 `GET /api/v1/stock/600519`
3. 验证仍返回分析数据

#### 预期结果
- API 返回 200
- 数据完整
- 后端日志记录缓存写入失败警告
- `cache_info.cached = false`

---

### TC-CACHE-020: 缓存读取失败降级

**优先级**: P1 - High
**类别**: 缓存/容错
**功能**: 验证缓存读取失败时降级到实时获取

#### 测试步骤
1. 模拟 Supabase 读取失败
2. 调用 `GET /api/v1/stock/600519?refresh=false`
3. 验证仍返回数据

#### 预期结果
- API 返回 200
- 自动降级到实时获取数据
- 响应时间 15-30 秒 (无缓存)
- `cache_info.cached = false`

---

## 测试套件 B: AI 股票问答测试

---

### TC-CHAT-001: 预设问题模板 - 财报分析

**优先级**: P0 - Critical
**类别**: 问答/预设模板
**功能**: 验证财报分析模板

#### 测试步骤
1. 调用 `POST /api/v1/stock/600519/chat`
```json
{
  "user_id": "test_user",
  "question": "财报表现怎么样?",
  "template": "financial"
}
```
2. 验证响应格式和内容

#### 预期结果
```json
{
  "code": "600519",
  "question": "财报表现怎么样?",
  "answer": "根据最新财报数据，贵州茅台...",
  "context_type": "financial",
  "tokens_used": 650,
  "remaining_quota": 9,
  "created_at": "2026-02-09T20:30:00Z"
}
```

- HTTP 状态码: 200
- `answer` 长度 50-500 字
- 包含财报相关关键词: ROE、营收、净利润
- `tokens_used` 在 300-1000 之间
- `remaining_quota` 正确递减

---

### TC-CHAT-002: 预设问题模板 - 近期资讯

**优先级**: P0 - Critical
**类别**: 问答/预设模板
**功能**: 验证新闻资讯模板

#### 测试步骤
1. 调用问答 API，`template: "news"`
2. 验证回答包含近期新闻

#### 预期结果
- 回答引用近期新闻标题
- 包含日期信息
- Token 消耗 500-1000

---

### TC-CHAT-003: 预设问题模板 - 同行对比

**优先级**: P1 - High
**类别**: 问答/预设模板
**功能**: 验证行业对比模板

#### 测试步骤
1. 调用问答 API，`template: "comparison"`
2. 验证回答包含同行业公司对比

#### 预期结果
- 回答提及同行业公司名称
- 包含对比指标 (涨跌幅、估值等)

---

### TC-CHAT-004: 预设问题模板 - 技术解读

**优先级**: P1 - High
**类别**: 问答/预设模板
**功能**: 验证技术指标解读模板

#### 测试步骤
1. 调用问答 API，`template: "technical"`
2. 验证回答解读技术指标

#### 预期结果
- 回答包含 MACD、RSI、MA 等指标解读
- 使用易懂的语言
- 不直接给出买卖建议

---

### TC-CHAT-005: 预设问题模板 - 风险提示

**优先级**: P1 - High
**类别**: 问答/预设模板
**功能**: 验证风险提示模板

#### 测试步骤
1. 调用问答 API，`template: "risk"`
2. 验证回答包含风险提示

#### 预期结果
- 回答列举潜在风险
- 包含风险等级评估
- 末尾有免责声明

---

### TC-CHAT-006: 自由提问功能

**优先级**: P0 - Critical
**类别**: 问答/自由提问
**功能**: 验证用户自定义问题

#### 测试步骤
1. 调用问答 API，无 `template` 参数
```json
{
  "user_id": "test_user",
  "question": "这家公司的护城河是什么?"
}
```
2. 验证 GLM-4 正确理解并回答

#### 预期结果
- 返回相关的回答
- 回答围绕股票上下文
- 不偏离主题
- Token 消耗 500-1200

---

### TC-CHAT-007: Token 成本验证

**优先级**: P0 - Critical
**类别**: 问答/Token 监控
**功能**: 验证 Token 消耗记录准确

#### 测试步骤
1. 查询当前 Token 使用量 `GET /api/v1/token/usage/today`
2. 记录 `total_tokens` 为 T1
3. 调用问答 API
4. 记录响应中的 `tokens_used` 为 N
5. 再次查询 Token 使用量，记录为 T2
6. 验证 T2 = T1 + N

#### 预期结果
- Token 记录准确
- 误差 < 5%

---

### TC-CHAT-008: 频率限制 - 每日 10 次

**优先级**: P0 - Critical
**类别**: 问答/频率控制
**功能**: 验证每日问答次数限制

#### 测试步骤
1. 使用同一 `user_id` 连续调用问答 API 11 次
2. 验证第 11 次请求被拒绝

#### 预期结果
```json
// 第 10 次成功
{
  "remaining_quota": 0
}

// 第 11 次失败
{
  "error": "今日问答额度已用完，明日可继续使用",
  "status_code": 429
}
```

- HTTP 状态码: 429 Too Many Requests
- 错误信息友好

---

### TC-CHAT-009: 频率限制 - 跨天重置

**优先级**: P1 - High
**类别**: 问答/频率控制
**功能**: 验证次日额度重置

#### 测试步骤
1. 使用完今日 10 次额度
2. 手动调整系统时间到次日 (或等待次日)
3. 再次调用问答 API

#### 预期结果
- 次日额度恢复为 10 次
- 可以正常提问

---

### TC-CHAT-010: 相似问题缓存命中

**优先级**: P1 - High
**类别**: 问答/缓存优化
**功能**: 验证相同问题 2 小时内返回缓存

#### 测试步骤
1. 提问 "这家公司最近有什么利好?"
2. 等待 30 秒
3. 再次提问同样的问题
4. 验证返回缓存回答

#### 预期结果
- 第二次响应时间 < 1 秒 (缓存命中)
- 回答内容与第一次相同
- `tokens_used = 0` (未调用 GLM-4)
- 不消耗问答次数额度

---

### TC-CHAT-011: 相似问题关键词匹配

**优先级**: P1 - High
**类别**: 问答/相似匹配
**功能**: 验证关键词重叠率匹配

#### 测试步骤
1. 提问 "公司最近有什么重大事件?"
2. 提问 "最近有什么重大事件发生?" (70% 关键词重叠)
3. 验证第二次返回缓存

#### 预期结果
- 关键词重叠率 > 70% 时命中缓存
- 返回相同答案

#### 测试变体
| 问题 1 | 问题 2 | 关键词重叠率 | 预期 |
|-------|-------|------------|------|
| "公司最近有什么利好?" | "最近有什么利好消息?" | 80% | 缓存命中 |
| "公司最近有什么利好?" | "这家公司的财报怎么样?" | 30% | 缓存未命中 |

---

### TC-CHAT-012: 问答历史查询

**优先级**: P1 - High
**类别**: 问答/历史记录
**功能**: 验证历史问答查询

#### 测试步骤
1. 提问 3 个不同的问题
2. 调用 `GET /api/v1/stock/600519/chat/history?user_id=test_user&limit=5`
3. 验证返回历史记录

#### 预期结果
```json
{
  "code": "600519",
  "count": 3,
  "history": [
    {
      "id": "uuid",
      "question": "...",
      "answer": "...",
      "context_type": "news",
      "tokens_used": 650,
      "created_at": "2026-02-09T20:30:00Z"
    },
    ...
  ]
}
```

- 返回最近 3 条记录
- 按时间倒序排列
- 所有字段完整

---

### TC-CHAT-013: GLM-4 不可用降级

**优先级**: P0 - Critical
**类别**: 问答/容错
**功能**: 验证 GLM-4 失败时降级处理

#### 测试步骤
1. 模拟 GLM-4 API 不可用 (断网或 API Key 无效)
2. 调用问答 API
3. 验证降级响应

#### 预期结果
- HTTP 状态码: 503 Service Unavailable
- 错误信息: "AI 服务暂时不可用，请稍后重试"
- 不消耗问答次数
- 后端日志记录错误

---

### TC-CHAT-014: GLM-4 超时处理

**优先级**: P1 - High
**类别**: 问答/超时
**功能**: 验证 GLM-4 超时降级

#### 测试步骤
1. 设置 GLM-4 超时时间为 5 秒
2. 提问复杂问题 (触发 GLM-4 处理时间长)
3. 验证超时后返回错误

#### 预期结果
- 5 秒后返回超时错误
- 不阻塞其他请求
- 消耗问答次数 (已记录到历史)

---

### TC-CHAT-015: 问题长度限制

**优先级**: P1 - High
**类别**: 问答/输入验证
**功能**: 验证问题长度 200 字限制

#### 测试步骤
1. 提交 201 字的问题
2. 验证被拒绝

#### 预期结果
- HTTP 状态码: 422 Unprocessable Entity
- 错误信息: "问题长度不能超过 200 字"
- 不消耗额度

---

### TC-CHAT-016: 回答长度控制

**优先级**: P1 - High
**类别**: 问答/Token 控制
**功能**: 验证 AI 回答长度在 200 字以内

#### 测试步骤
1. 提问任意问题
2. 验证回答长度

#### 预期结果
- 回答长度 50-400 字 (控制在 200 字为主)
- `max_tokens=400` 生效
- Token 消耗可控

---

### TC-CHAT-017: 前端 StockChatPanel 组件

**优先级**: P1 - High
**类别**: 问答/前端
**功能**: 验证问答面板 UI

#### 测试步骤
1. 访问股票详情页 `/stock/600519`
2. 滚动到 "AI 股票问答" 区域
3. 检查 UI 元素

#### 预期元素
- 5 个预设问题按钮: [财报分析] [近期资讯] [同行对比] [技术解读] [风险提示]
- 输入框和 [发送] 按钮
- 问答历史记录区域
- "今日已提问 X/10 次" 提示

---

### TC-CHAT-018: 前端点击预设问题

**优先级**: P1 - High
**类别**: 问答/前端交互
**功能**: 验证点击预设问题自动提交

#### 测试步骤
1. 点击 [财报分析] 按钮
2. 观察发送状态
3. 等待回答返回

#### 预期结果
- 按钮变为加载状态
- 输入框禁用
- 回答流式显示或完整返回
- 显示 "Token 消耗: 650 | 今日剩余: 8/10 次"

---

### TC-CHAT-019: 前端额度用尽提示

**优先级**: P1 - High
**类别**: 问答/前端提示
**功能**: 验证额度用尽时 UI 变化

#### 测试步骤
1. 连续提问 10 次
2. 验证第 10 次后输入框状态

#### 预期结果
- 输入框和发送按钮禁用
- 显示提示: "今日问答额度已用完，明日可继续使用"
- 预设问题按钮禁用

---

### TC-CHAT-020: 前端问答历史显示

**优先级**: P2 - Medium
**类别**: 问答/前端历史
**功能**: 验证历史记录显示

#### 测试步骤
1. 提问 3 个问题
2. 刷新页面
3. 验证历史记录加载

#### 预期结果
- 页面加载时自动加载最近 5 条历史
- 每条记录显示问题和回答
- 点击 "查看更多" 加载更多历史

---

## 测试套件 C: 性能测试

---

### TC-PERF-001: 缓存命中响应时间

**优先级**: P0 - Critical
**类别**: 性能/缓存命中
**功能**: 验证缓存命中时响应 < 1 秒

#### 测试步骤
1. 确保数据库中有缓存
2. 调用 `GET /api/v1/stock/600519?refresh=false`
3. 重复 10 次，记录平均响应时间

#### 预期结果
- 平均响应时间 < 1 秒
- 95% 请求 < 1.5 秒
- 无超时

---

### TC-PERF-002: 缓存未命中响应时间

**优先级**: P1 - High
**类别**: 性能/缓存未命中
**功能**: 验证完整分析响应时间可接受

#### 测试步骤
1. 清空缓存
2. 调用 `GET /api/v1/stock/600519?refresh=true`
3. 记录响应时间

#### 预期结果
- 响应时间 < 30 秒
- 平均 15-20 秒
- 包含完整 AI 分析

---

### TC-PERF-003: 批量行情 API 性能

**优先级**: P0 - Critical
**类别**: 性能/批量 API
**功能**: 验证批量查询比串行快

#### 测试步骤
1. 批量查询 10 只股票: `GET /api/v1/quotes/batch?codes=...`
2. 记录响应时间 T1
3. 串行查询 10 只股票 (逐个调用 `/stock/{code}`)
4. 记录总耗时 T2
5. 对比 T1 和 T2

#### 预期结果
- T1 < 3 秒
- T2 > 15 秒
- 性能提升 > 80%

---

### TC-PERF-004: 推荐列表加载时间

**优先级**: P0 - Critical
**类别**: 性能/列表加载
**功能**: 验证推荐列表 < 3 秒

#### 测试步骤
1. 调用 `GET /api/v1/recommendations`
2. 记录响应时间

#### 预期结果
- 响应时间 < 3 秒 (原来 11-21 秒)
- 包含 10 只股票的实时价格

---

### TC-PERF-005: 自选股列表加载时间

**优先级**: P0 - Critical
**类别**: 性能/列表加载
**功能**: 验证自选股列表 < 3 秒

#### 测试步骤
1. 添加 10 只股票到自选股
2. 访问自选股列表页
3. 记录加载时间

#### 预期结果
- 响应时间 < 3 秒 (原来 150-300 秒)
- 包含所有自选股的实时价格和缓存分析

---

### TC-PERF-006: 问答响应时间

**优先级**: P1 - High
**类别**: 性能/问答
**功能**: 验证问答响应时间可接受

#### 测试步骤
1. 提问 10 次
2. 记录每次响应时间
3. 计算平均值

#### 预期结果
- 平均响应时间 < 8 秒
- 95% 请求 < 12 秒
- 缓存命中时 < 1 秒

---

### TC-PERF-007: 缓存预热任务耗时

**优先级**: P1 - High
**类别**: 性能/调度任务
**功能**: 验证预热任务 < 5 分钟

#### 测试步骤
1. 准备 20 只股票需要预热
2. 执行缓存预热任务
3. 记录总耗时

#### 预期结果
- 总耗时 < 5 分钟
- 平均每只股票 < 15 秒

---

### TC-PERF-008: 内存使用监控

**优先级**: P1 - High
**类别**: 性能/内存
**功能**: 验证内存使用 < 300MB

#### 测试步骤
1. 启动后端服务
2. 执行缓存预热
3. 监控内存使用

#### 预期结果
- 空闲时内存 < 250MB
- 峰值内存 < 300MB
- 无内存泄漏

---

### TC-PERF-009: Supabase 查询延迟

**优先级**: P1 - High
**类别**: 性能/数据库
**功能**: 验证数据库查询延迟可接受

#### 测试步骤
1. 直接查询 `stock_analysis_cache` 表
2. 记录查询时间

#### 预期结果
- 单条查询 < 200ms
- 批量查询 (10条) < 500ms

---

### TC-PERF-010: 并发请求性能

**优先级**: P2 - Medium
**类别**: 性能/并发
**功能**: 验证 10 个并发请求不超时

#### 测试步骤
1. 同时发送 10 个股票查询请求
2. 验证所有请求成功返回

#### 预期结果
- 所有请求成功
- 平均响应时间 < 3 秒
- 无服务崩溃

---

## 测试套件 D: 边界和异常测试

---

### TC-EDGE-001: 缓存数据库连接失败

**优先级**: P0 - Critical
**类别**: 边界/容错
**功能**: 验证数据库不可用时降级

#### 测试步骤
1. 模拟 Supabase 不可用
2. 调用 `GET /api/v1/stock/600519`

#### 预期结果
- API 仍返回数据 (实时获取)
- `cache_info.cached = false`
- 后端日志记录错误

---

### TC-EDGE-002: 批量行情部分失败

**优先级**: P1 - High
**类别**: 边界/批量 API
**功能**: 验证部分股票失败时处理

#### 测试步骤
1. 批量查询包含 1 只无效代码: `codes=600519,999999,000001`
2. 验证响应

#### 预期结果
- 返回成功获取的 2 只股票
- 失败的股票标记为 null 或跳过
- 不影响其他股票

---

### TC-EDGE-003: 问答 GLM-4 返回空内容

**优先级**: P1 - High
**类别**: 边界/问答
**功能**: 验证 GLM-4 返回空时处理

#### 测试步骤
1. 模拟 GLM-4 返回空字符串
2. 验证 API 响应

#### 预期结果
- 返回默认回答: "AI 暂时无法回答此问题，请稍后重试"
- 不消耗问答次数
- 记录错误日志

---

### TC-EDGE-004: 问答历史查询空结果

**优先级**: P2 - Medium
**类别**: 边界/问答
**功能**: 验证无历史记录时返回空数组

#### 测试步骤
1. 查询从未提问过的股票历史
2. 验证响应

#### 预期结果
```json
{
  "code": "600519",
  "count": 0,
  "history": []
}
```

---

### TC-EDGE-005: 缓存过期边界 - 4 小时整

**优先级**: P1 - High
**类别**: 边界/缓存过期
**功能**: 验证刚好 4 小时的边界情况

#### 测试步骤
1. 插入缓存记录，`full_analysis_updated_at = NOW() - INTERVAL '4 hours'`
2. 调用 API 验证是否过期

#### 预期结果
- 刚好 4 小时应判定为过期
- 触发重新分析

---

### TC-EDGE-006: 实时价格缓存边界 - 30 秒整

**优先级**: P1 - High
**类别**: 边界/缓存过期
**功能**: 验证刚好 30 秒的边界情况

#### 测试步骤
1. 第一次调用记录时间
2. 刚好 30 秒后调用

#### 预期结果
- 30 秒应判定为过期
- 重新获取实时价格

---

### TC-EDGE-007: 问答次数边界 - 第 10 次

**优先级**: P1 - High
**类别**: 边界/频率限制
**功能**: 验证第 10 次请求成功，第 11 次失败

#### 测试步骤
1. 连续提问 11 次
2. 验证第 10 次成功，第 11 次失败

#### 预期结果
- 第 10 次: 200, `remaining_quota: 0`
- 第 11 次: 429, 错误提示

---

### TC-EDGE-008: 缓存预热期间查询

**优先级**: P2 - Medium
**类别**: 边界/并发
**功能**: 验证预热期间用户查询不冲突

#### 测试步骤
1. 启动缓存预热任务
2. 同时用户查询同一股票

#### 预期结果
- 两个请求都成功
- 无锁冲突
- 缓存最终一致

---

### TC-EDGE-009: 空问题提交

**优先级**: P2 - Medium
**类别**: 边界/输入验证
**功能**: 验证空问题被拒绝

#### 测试步骤
1. 提交 `question: ""`
2. 验证响应

#### 预期结果
- HTTP 422
- 错误: "问题不能为空"

---

### TC-EDGE-010: Token 监控统计准确性

**优先级**: P1 - High
**类别**: 边界/Token 监控
**功能**: 验证 Token 统计无遗漏

#### 测试步骤
1. 记录初始 Token 总数
2. 执行 5 次问答
3. 验证 Token 增量 = 5 次问答的总和

#### 预期结果
- Token 统计准确
- 误差 < 5%

---

## 5. 测试执行计划

### 5.1 执行顺序

```
阶段 1: 环境准备和冒烟测试 (15 分钟)
  ├─ 验证数据库表创建
  ├─ 验证后端服务正常
  ├─ TC-V3-SMOKE-001 ~ 005
  └─ 环境问题修复

阶段 2: 缓存功能测试 (30 分钟)
  ├─ TC-CACHE-001 ~ TC-CACHE-010 (核心功能)
  ├─ TC-CACHE-011 ~ TC-CACHE-015 (高级功能)
  └─ TC-CACHE-016 ~ TC-CACHE-020 (容错和前端)

阶段 3: 问答功能测试 (30 分钟)
  ├─ TC-CHAT-001 ~ TC-CHAT-007 (核心功能)
  ├─ TC-CHAT-008 ~ TC-CHAT-013 (频率和容错)
  └─ TC-CHAT-014 ~ TC-CHAT-020 (边界和前端)

阶段 4: 性能测试 (20 分钟)
  ├─ TC-PERF-001 ~ TC-PERF-005 (响应时间)
  └─ TC-PERF-006 ~ TC-PERF-010 (资源使用)

阶段 5: 边界和异常测试 (15 分钟)
  └─ TC-EDGE-001 ~ TC-EDGE-010

阶段 6: E2E 场景测试 (10 分钟)
  └─ E2E-V3-001 ~ E2E-V3-003

总计: 120 分钟 (2 小时)
```

### 5.2 测试数据准备

| 数据类型 | 准备内容 |
|---------|---------|
| 股票代码 | 600519, 000001, 512930, 999999 (无效) |
| 用户 ID | test_user, user_001, user_002 |
| 缓存记录 | 预先插入 1 小时前、5 小时前的缓存 |
| 问答历史 | 预先插入 5 条历史问答 |
| 时间点 | 交易时段 (10:00)、收盘后 (16:00)、周末 |

### 5.3 测试工具

#### Python 测试脚本模板

```python
#!/usr/bin/env python3
"""
Stock Advisor v3.0 测试脚本 - 缓存和问答功能
"""

import requests
import time
from datetime import datetime, timedelta

BASE_URL = "https://stock-advisor-api-6vtb.onrender.com"
TEST_CODE = "600519"
TEST_USER = "test_user"

class V3TestRunner:
    def __init__(self):
        self.results = []
        self.passed = 0
        self.failed = 0

    def test_cache_hit(self):
        """TC-CACHE-003: 验证缓存命中"""
        print("[TEST] TC-CACHE-003: 缓存命中逻辑验证")

        # 第一次调用
        start = time.time()
        r1 = requests.get(f"{BASE_URL}/api/v1/stock/{TEST_CODE}?refresh=false")
        t1 = time.time() - start
        data1 = r1.json()

        time.sleep(5)

        # 第二次调用 (应缓存命中)
        start = time.time()
        r2 = requests.get(f"{BASE_URL}/api/v1/stock/{TEST_CODE}?refresh=false")
        t2 = time.time() - start
        data2 = r2.json()

        # 验证
        assert data2.get("cache_info", {}).get("cached") == True, "应命中缓存"
        assert t2 < 1.0, f"缓存命中响应应 < 1s，实际: {t2:.2f}s"
        assert data1["full_analysis_updated_at"] == data2["full_analysis_updated_at"], "分析时间应相同"

        print(f"✅ PASS: 缓存命中，响应时间 {t2:.2f}s")
        self.passed += 1

    def test_batch_quotes(self):
        """TC-CACHE-007: 验证批量行情 API"""
        print("[TEST] TC-CACHE-007: 批量行情 API")

        codes = "600519,000001,512930"
        start = time.time()
        r = requests.get(f"{BASE_URL}/api/v1/quotes/batch?codes={codes}")
        elapsed = time.time() - start
        data = r.json()

        # 验证
        assert r.status_code == 200, f"HTTP {r.status_code}"
        assert data["count"] == 3, f"应返回 3 只股票，实际: {data['count']}"
        assert elapsed < 3.0, f"响应时间应 < 3s，实际: {elapsed:.2f}s"

        print(f"✅ PASS: 批量获取 3 只股票，耗时 {elapsed:.2f}s")
        self.passed += 1

    def test_chat_basic(self):
        """TC-CHAT-001: 验证预设问题模板"""
        print("[TEST] TC-CHAT-001: 财报分析模板")

        payload = {
            "user_id": TEST_USER,
            "question": "财报表现怎么样?",
            "template": "financial"
        }

        r = requests.post(f"{BASE_URL}/api/v1/stock/{TEST_CODE}/chat", json=payload)
        data = r.json()

        # 验证
        assert r.status_code == 200, f"HTTP {r.status_code}"
        assert "answer" in data, "缺少 answer 字段"
        assert len(data["answer"]) > 50, "回答过短"
        assert data["tokens_used"] > 0, "Token 未记录"
        assert data["context_type"] == "financial", "上下文类型错误"

        print(f"✅ PASS: 问答成功，Token: {data['tokens_used']}, 剩余: {data['remaining_quota']}")
        self.passed += 1

    def test_chat_rate_limit(self):
        """TC-CHAT-008: 验证频率限制"""
        print("[TEST] TC-CHAT-008: 问答频率限制")

        # 连续提问 11 次
        for i in range(11):
            payload = {
                "user_id": f"limit_test_user",
                "question": f"测试问题 {i+1}"
            }
            r = requests.post(f"{BASE_URL}/api/v1/stock/{TEST_CODE}/chat", json=payload)

            if i < 10:
                assert r.status_code == 200, f"第 {i+1} 次应成功，实际: {r.status_code}"
            else:
                assert r.status_code == 429, f"第 11 次应被限制，实际: {r.status_code}"
                print(f"✅ PASS: 第 11 次请求被正确拒绝 (429)")

        self.passed += 1

    def test_cache_expiration(self):
        """TC-CACHE-009: 验证缓存过期策略"""
        print("[TEST] TC-CACHE-009: 缓存 4 小时过期")

        # 需要手动在数据库插入过期缓存记录
        # 或等待真实时间验证
        print("⏭️ SKIP: 需要手动插入过期缓存记录")

    def run_all(self):
        """运行所有测试"""
        print("=" * 60)
        print("Stock Advisor v3.0 自动化测试")
        print("=" * 60)

        self.test_cache_hit()
        self.test_batch_quotes()
        self.test_chat_basic()
        self.test_chat_rate_limit()
        # self.test_cache_expiration()

        print("\n" + "=" * 60)
        print(f"测试完成: {self.passed} 通过, {self.failed} 失败")
        print("=" * 60)

if __name__ == "__main__":
    runner = V3TestRunner()
    runner.run_all()
```

---

## 6. 缺陷管理

### 6.1 严重级别定义

| 级别 | 定义 | 示例 |
|-----|------|------|
| CRITICAL | 核心功能不可用 | 缓存写入失败导致服务崩溃 |
| MAJOR | 功能不符合设计 | 缓存 4 小时后未过期 |
| MINOR | 小问题，不影响主流程 | 前端缓存提示样式错误 |
| TRIVIAL | 美化性问题 | UI 字体大小 |

### 6.2 已知风险问题

| ID | 严重级别 | 描述 | 状态 | 缓解措施 |
|----|---------|------|------|---------|
| RISK-V3-001 | MAJOR | Supabase 延迟 > 2s 导致缓存优化失效 | 监控中 | 内存缓存兜底 |
| RISK-V3-002 | MINOR | 相似问题匹配算法误判率 > 10% | 待验证 | 调整阈值 |
| RISK-V3-003 | MAJOR | 缓存预热任务失败无告警 | 待实现 | 添加任务监控 |

---

## 7. 验收标准清单

### 7.1 生产就绪检查清单

**功能完整性**:
- [ ] 所有 P0 测试用例 100% 通过
- [ ] 所有 P1 测试用例 ≥ 95% 通过
- [ ] 缓存和问答核心功能可用

**性能指标**:
- [ ] 缓存命中响应 < 1 秒
- [ ] 批量行情 API < 3 秒
- [ ] 推荐列表加载 < 3 秒
- [ ] 自选股列表加载 < 3 秒
- [ ] 问答响应 < 10 秒

**数据准确性**:
- [ ] 缓存时间戳准确
- [ ] Token 统计准确
- [ ] 频率限制生效

**错误处理**:
- [ ] 缓存失败降级正常
- [ ] GLM-4 失败降级正常
- [ ] 数据库不可用时仍可返回数据

**用户体验**:
- [ ] 前端缓存提示清晰
- [ ] 问答面板可用
- [ ] 额度提示友好

### 7.2 发布阻塞问题

**必须修复 (P0)**:
- 🔴 缓存写入失败导致服务崩溃
- 🔴 问答频率限制失效
- 🔴 批量行情 API 不可用

**强烈建议修复 (P1)**:
- 🟡 缓存预热任务失败无告警
- 🟡 相似问题匹配误判率高

---

## 8. 测试执行记录

### 8.1 测试轮次记录

| 轮次 | 日期 | 执行人 | 通过率 | 问题数 |
|-----|------|-------|-------|-------|
| Round 1 | 2026-02-09 | Test Expert | 待执行 | - |
| Round 2 | 待定 | 待定 | - | - |

### 8.2 问题修复跟踪

| 问题 ID | 报告日期 | 修复日期 | 验证日期 | 状态 |
|--------|---------|---------|---------|------|
| - | - | - | - | - |

---

## 9. 附录

### 9.1 参考文档
- [DESIGN_V3_CACHE_AND_QA.md](/Users/derek/Documents/macbookair_files/AI_path/projects/software/stock-advisor/DESIGN_V3_CACHE_AND_QA.md) - v3.0 设计文档
- [TEST_CASES.md](/Users/derek/Documents/macbookair_files/AI_path/projects/software/stock-advisor/TEST_CASES.md) - v1.0/v2.0 测试用例
- [test_e2e_v2.py](/Users/derek/Documents/macbookair_files/AI_path/projects/software/stock-advisor/backend/test_e2e_v2.py) - v2.0 E2E 测试脚本

### 9.2 测试术语
- **P0**: 最高优先级，阻塞发布
- **P1**: 高优先级，强烈建议修复
- **P2**: 中等优先级，可延后修复
- **缓存命中**: Cache Hit，从缓存读取数据
- **缓存未命中**: Cache Miss，缓存不存在或过期，需重新获取
- **TTL**: Time To Live，缓存有效期

---

**文档版本**: v3.0
**创建日期**: 2026-02-09
**作者**: Test Expert
**下次更新**: 执行测试后更新结果
